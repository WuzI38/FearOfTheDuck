namespace Constants 
{
    // Id of every single floor tile
    public enum tileType {
        floor,
        wall,
        start,
        exit,
        spike,
    }

    // Id of every single gun
    public enum itemType {
        Null,
        Health,
        Pistol,
        Rifle,
        Shotgun,
    }
}