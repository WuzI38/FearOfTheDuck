namespace Constants 
{
    public enum tileType {
        floor,
        wall,
        start,
        exit,
        spike,
    }

    public enum itemType {
        Null,
        Health,
        Pistol,
        Rifle,
        Shotgun,
    }
}